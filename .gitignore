# Dépendances
node_modules/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Fichiers de config locale (contenant potentiellement des secrets)
config.local.js
worker/.dev.vars
js/config.local.js

# OS
.DS_Store
Thumbs.db

# ZCode (fichiers internes de l'agent)
.zcode/
.v2c/

# Sauvegardes / copies
*Copie*
*.bak
*.orig

# Builds générés
dist/

# Aperçus PDF/PNG générés (test/gen-apercu-navlog.mjs) et profils Edge headless
Apercu_*.pdf
Apercu_*.png
.edge-*/

# Déploiement FTP (identifiants + état)
deploy.config.json
deploy.state.json

# Dépendances npm (projet sans lock tracé)
package-lock.json

# eAIP téléchargé (local, jamais committé)
eaip_06_aug_2026/
eaip_*/
*.zip

# Sources SIA volumineuses locales (ré-téléchargeables depuis le portail SIA)
telechargement AIRAC/donnees_tod_france_sia_1/
telechargement AIRAC/*.pdf
# Audit 26/09 : les XML bruts (AIXM ~44 Mo + XML_SIA ~27 Mo PAR CYCLE)
# ne sont plus suivis — ils s'empilaient à chaque AIRAC (137 Mo à ce jour).
# Ils restent sur disque pour fetch-sia-airac/fetch-obstacles (usage local).
telechargement AIRAC/*.xml

# Cellules openAIP régénérables (fetch-airspaces.mjs + npm run cells) :
# 26 725 fichiers / 252 Mo suivis pour rien — jamais déployées par le FTP
# standard (DENIED), uploadées à part. Dépôt allégé de ~380 Mo au clone.
# NOTE : l'historique git les garde jusqu'à un filter-repo (à faire APRÈS
# la bascule AIRAC du 01/10/2026, qui dépend du ref a937cce3).
data/airspaces/cells/

# Captures QA locales (rendu navigateur — régénérables)
test/render/
test/*.png

